server:
  port: 9091
spring:
  application:
    name: Wipro-Security
  datasource:
    url: jdbc:mysql://localhost:3306/wipro_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true